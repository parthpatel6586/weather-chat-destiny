{
  "name": "weather-chat-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Small proxy server that talks to the Gemini API so the API key never reaches the browser.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  }
}
